name: Release

on:
  workflow_dispatch:

jobs:
  version:
    name: Compute version
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.compute.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Compute version
        id: compute
        run: |
          BASE=$(grep 'TOOLS_VERSION_BASE' CMakeLists.txt | grep -oP '"[0-9]+\.[0-9]+"' | tr -d '"')
          COUNT=$(git rev-list HEAD --count)
          echo "tag=v${BASE}.${COUNT}" >> $GITHUB_OUTPUT

  build:
    name: ${{ matrix.name }}
    needs: version
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            name: linux-x64
            configure_preset: vcpkg-release
            build_dir: build/release
          - os: macos-latest
            name: macos-arm64
            configure_preset: vcpkg-release
            build_dir: build/release
          - os: windows-2022
            name: windows-x64
            configure_preset: vcpkg-msvc-release
            build_dir: build/msvc-release

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          sudo apt-get install -y ninja-build pkg-config

      - name: Enable long paths
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
            -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11.6
        with:
          vcpkgGitCommitId: '66c0373dc7fca549e5803087b9487edfe3aca0a1'

      - name: Setup MSVC environment
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure
        run: cmake --preset ${{ matrix.configure_preset }}

      - name: Build
        run: cmake --build ${{ matrix.build_dir }} --parallel

      - name: Install
        run: cmake --install ${{ matrix.build_dir }} --prefix staging

      - name: Package (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          tar czf tools-${{ needs.version.outputs.tag }}-${{ matrix.name }}.tar.gz \
            -C staging/bin .

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path staging/bin/* `
            -DestinationPath "tools-${{ needs.version.outputs.tag }}-${{ matrix.name }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tools-${{ needs.version.outputs.tag }}-${{ matrix.name }}
          path: tools-${{ needs.version.outputs.tag }}-${{ matrix.name }}.*
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: [version, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          gh release create ${{ needs.version.outputs.tag }} \
            --title "Release ${{ needs.version.outputs.tag }}" \
            --generate-notes \
            artifacts/*
